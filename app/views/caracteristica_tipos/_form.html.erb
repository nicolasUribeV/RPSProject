<%= form_for(@caracteristica_tipo) do |f| %>
  <% if @caracteristica_tipo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@caracteristica_tipo.errors.count, "error") %> prohibited this caracteristica_tipo from being saved:</h2>

      <ul>
      <% @caracteristica_tipo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :TipoPropiedad_id %><br>
    <%= f.text_field :TipoPropiedad_id %>
  </div>
  <div class="field">
    <%= f.label :Caracteristica_id %><br>
    <%= f.text_field :Caracteristica_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
