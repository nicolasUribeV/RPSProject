<h1>Buscar Propiedades</h1>

<%= f.collection_select :Usuario_id, Usuario.joins("LEFT JOIN arrendatarios ON usuarios.idS = arrendatarios.usuario_id").where("arrendatarios.usuario_id IS NULL").order(:Nombre), :id, :Nombre, include_blank: false %>
<%= f.submit  %>

<table>
  <thead>
    <tr>
      <th>Ubicacion</th>
      <th>Tipopropiedad</th>
      <th>Propietario</th>
      <th>Direccionpropiedad</th>
      <th>Estacionamiento</th>
      <th>Bodega</th>
      <th>Equipadamuebles</th>
      <th>M2totales</th>
      <th>Descripcion</th>
      <th>Habilitada</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @propiedads.each do |propiedad| %>
      <tr>
        <td><%= propiedad.Ubicacion %></td>
        <td><%= propiedad.TipoPropiedad %></td>
        <td><%= propiedad.Propietario %></td>
        <td><%= propiedad.DireccionPropiedad %></td>
        <td><%= propiedad.Estacionamiento %></td>
        <td><%= propiedad.Bodega %></td>
        <td><%= propiedad.EquipadaMuebles %></td>
        <td><%= propiedad.M2Totales %></td>
        <td><%= propiedad.Descripcion %></td>
        <td><%= propiedad.Habilitada %></td>
        <td><%= link_to 'Show', propiedad %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>