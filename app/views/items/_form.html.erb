<%= form_for(@item) do |f| %>
  <% if @item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@item.errors.count, "error") %> prohibited this item from being saved:</h2>

      <ul>
      <% @item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :NombreItem %><br>
    <%= f.text_field :NombreItem %>
  </div>
  <div class="field">
    <%= f.label :DescripcionItem %><br>
    <%= f.text_area :DescripcionItem %>
  </div>
  <div class="field">
    <%= f.label :EstadoItem %><br>
    <%= f.check_box :EstadoItem %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
